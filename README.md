# tiny_array

Небольшая утилита на C++, создающая массив TinyArray без использования std::vector.

## Что делает программа

Программа читает количество элементов `n`, затем `n` целых чисел, записывает эти числа в массив, после чего они доступны для чтения